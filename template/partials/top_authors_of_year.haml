%h2.blockheader
  Author of year
.blockcontainer
  .blocktable
    %table
      %thead
        %th Year
        %th Author
        %th Commits
        %th Total
      %tbody
        - stats.year_stats.years.sort{ |a, b| b[0] <=> a[0] }.each do |year, authorstats|
          - authors = authorstats.authors.sort{ |a, b| b[1].commits <=> a[1].commits }[0, 5]
          - authors.each do |author|
            - c = author[1].commits
            - first = author == authors.first
            %tr{:class => first ? 'top' : ''}
              %td&= first ? year : ''
              %td&= author[0]
              %td&= '%d of %d (%.2f%%)' % [c, authorstats.commits.to_f, c * 100.0 / authorstats.commits.to_f]
              %td&= first ? authorstats.authors.count : ''
