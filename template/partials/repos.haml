- block(:repos) do
  .blocktable
    %table.simpletable
      %thead
        %th Path
        %th Ref
      %tbody
        - stats.repos.each do |repo|
          %tr
            %td&= repo.base
            %td&= repo.ref
